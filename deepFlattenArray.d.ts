/**
 * 深度扁平化数组，会将数组变化后为一层
 * @param {array} arr 要处理的数组
 * @param {number} depth 处理深度
 * @returns {array} result 返回扁平后的数组
 * @name deepFlattenArray
 */
export function deepFlattenArray(arr: any[], depth: number): any[]
